# Frontend (Vite) configuration.
#
# Base origin of the API server. Leave empty for local dev (Vite proxies /api
# to http://localhost:8787). In production (e.g. Vercel), set this to the
# deployed API origin, for example: https://snippet-showcase-api.onrender.com
VITE_API_URL=
